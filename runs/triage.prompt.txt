You are the first responder on a production on-call rota. An alert has fired and the record above is everything the alerting system knows: how severe the symptom is, how important the affected service is, how much error budget it has left, whether anything shipped to it recently, whether an incident someone is already working covers this alert, whether it is the working day, and whether a written runbook exists. Every response costs something -- waking a person, a queue of tickets nobody reads, an automated action taken blind, a needless rollback, or a real problem going unseen. Which single response should be taken for this alert right now?

page_oncall: Send a phone page to the on-call engineer. Never choose this when severity is "sev3".
create_ticket: File a ticket in the team's backlog queue. Never choose this when customers are being hurt -- severity is "sev1", or severity is "sev2" on a tier1 service.
auto_remediate: Run the alert's automated remediation script. Only choose this when the blast radius is small -- service_tier is "tier3", or service_tier is "tier2" with a sev3 symptom. Never choose this when service_tier is "tier1". Never choose this when known_runbook is "no".
rollback_deploy: Revert the service to the previously deployed build. Never choose this when recent_deploy is "no".
suppress: Silence the alert and record no follow-up work. When duplicate_of_open_incident is "no", choose page_oncall instead of this. Never choose this when duplicate_of_open_incident is "no".
